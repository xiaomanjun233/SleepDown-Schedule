# 1.2.6 Beta1 发布记录

用户要求发布 1.2.6 Beta1，并指定日志为「优化应用稳定性，提高应用流畅度」。本次包含此前已完成的三点菜单与玻璃冻结优化，详见 [性能记录](performance/2026-09-14-menu-freeze-coordinates.md)。

## 版本与日志

- 版本 `1.2.6_beta1`，versionCode `33`，包名 `com.xiaomanjun.sleepdownschedule`。
- 发布源码提交 `5cedd3a14fea8a58b05f1b1a1c4c37a985bc852c`，两端标签 `v1.2.6_beta1` 均指向该提交；源码已同步至 `codex/mainline-preview-performance`。
- 应用内更新日志新增当前 Beta，日期 `2026-09-14`，沿用当前版本默认展开。发布文案见 [Beta1 更新日志](../release-notes/v1.2.6_beta1.md)。
- 根目录 AGENTS.md 已记录后续规则：每个 Beta 同步编写应用内与发布页日志，正式版发布时再归并去重。

## 构建与发布验证

- `:app:assembleGithubRelease` 成功，耗时 4 分 15 秒，保留 R8、资源压缩、lintVital 与签名打包；本次没有重复运行性能测试。
- 最终 APK v2 签名校验通过；从安装包核对版本号、versionCode，并确认指定日志文案已包含在 DEX 中。
- 文件名 `SleepDown-Schedule_v1.2.6_beta1.apk`，大小 `7,127,399` 字节。
- SHA-256：`8f1ea4f5e8650d22dce80ea8432c8b9893e936f0fb474f5a9cbd3aae947d3ad7`。
- [GitHub Beta1](https://github.com/xiaomanjun233/SleepDown-Schedule/releases/tag/v1.2.6_beta1)：插件核对 `draft=false`、`prerelease=true`、源码提交、日志及 APK 摘要一致。正式版 latest 仍为 `v1.2.5`。
- [Gitee Beta1](https://gitee.com/xiaomanjun233/SleepDown-Schedule/releases/tag/v1.2.6_beta1)：`prerelease=true`；公开附件下载回读的大小与 SHA-256 一致。应用使用的版本列表接口已返回新版本、完整日志与 APK 附件。
- GitHub 插件用于元数据读取；插件没有发布创建及二进制上传接口，上传沿用既有脚本与凭据连接，凭据仅在进程内使用。
- 本次按发布要求完成打包与上传，没有额外安装或启动应用。
