# 2026-09-25 工作空间与版本基线整理记录

这份记录说明本轮**实际做过**的整理，以及仍保留的工作树。当前版本关系见[版本基线](RELEASE_MODEL.md)，以后每轮的操作顺序见[开发工作流](DEVELOPMENT_WORKFLOW.md)。本报告是日期快照，不替代代码和构建配置。

## 核对到的基线

- `origin/main` 在本轮开始时为 `19cbfff`；本轮文档在 `codex/workspace-baseline-cleanup` 上整理。当前正式标签是 `v1.2.6`，同轮 `v1.2.6_beta1` 至 `beta10` 及历史 `v1.2.6-exp*` 标签均保留。
- 主应用配置为 `com.xiaomanjun.sleepdownschedule`、`versionName=1.2.6`、`versionCode=33`。普通开发以 `main` 为唯一持续基线；`develop` 与 `exp` 不再接收新功能。
- 最初的 `1.01 beta` 至 `1.10 beta` 属于更早的独立历史序列。逐期变化以[应用内日志整理](EARLY_BETA_HISTORY.md)为准，不能与 `1.2.6_beta1` 至 `beta10` 混同。

## 本轮移动与归档

本地归档根目录为 `D:\个人资料\开发归档\SleepDown\2026-09-25`。用户决定亲自删除的项目已集中在 **`待删除`**，这里的文件尚未删除；需要保留原件的内容在 **`保留归档`**。两处都在仓库外，不进入 GitHub。

| 区域 | 内容 | 本轮统计 |
| --- | --- | ---: |
| 待删除 | 一处残留 Codex 工作树文件 | 20,444 文件，约 1,244 MiB |
| 待删除 | 开发日志与调试日志 | 253 文件，约 1,221 MiB |
| 待删除 | 调试截图、UI dump、根目录临时图片、Bug 裁图与一张未采用的图库副本 | 158 文件，约 251 MiB |
| 保留归档 | 发布 APK、调试 APK、数据库快照 | 32 文件，约 226 MiB |
| 保留归档 | 原始 `tmp` 资料及清点索引 | 27,009 文件，约 2,373 MiB |

上表来自归档目录文件计数与大小统计，四舍五入到 MiB。待删除区还有一个空的调试容器。APK、数据库与原始 `tmp` 资料保持私有，仅为防止误丢失而保留；`SleepDown-Server/` 未纳入整理、暂存或上传。

展示用素材另放在 `D:\个人资料\图片\SleepDown开发历程`，与仓库完全分开。`index.html` 使用中文浅色长卷，包含早期十个 Beta 的日志摘要、五月二十日起的原始课表截图、今昔滑块、Bug 现场和横向照片墙。照片墙收录 **259 张**经过核对的相关图，原图副本、来源文件名与 SHA-256 写在同目录索引中；私人生活、课程作业和无关应用截图没有加入。

## 本地 Git 支线

- 40 个已并入主线且没有独立工作树的本地短期分支从日常列表退出；名称清单保存在归档根目录的 `保留归档/merged-local-branches-retired.txt`。
- 13 个含独有提交或长期历史价值的分支建立 `archive/2026-09-25/*` 本地引用后，从日常维护路径停用。包含历史 `develop`、`exp` 和若干实验线；对应标签与提交仍可追溯。
- 远端分支、已发布标签和 Release 未删除、重写或合并。本轮整理不把旧实验线重新定义为当前开发入口。

## 仍注册的工作树

除了当前根目录，Git 仍记录九处工作树。它们可能关联其他 Codex 任务；有未提交内容或独有提交的路径不能作为垃圾清除。本轮没有强制删除、重置或覆盖这些树。

| 工作树末段 | 独有提交（相对 `origin/main`） | 本轮状态 | 保留原因 |
| --- | ---: | --- | --- |
| `auto-refresh-schedule` | 2 | 干净 | 有独有提交，先留作任务交接 |
| `beta10-oem-followup` | 0 | 干净 | 已合入；可能仍被任务引用 |
| `home-course-glass-render` | 0 | 干净 | 已合入；可能仍被任务引用 |
| `main-adjustment-badge` | 0 | 2 个修改文件 | 未提交改动 |
| `main-course-editing-widgets` | 0 | 干净 | 已合入；可能仍被任务引用 |
| `notification-weekjump-fix` | 18 | 7 个修改文件 | 独有提交与未提交改动 |
| `CourseSchedule-morph-current` | 2 | 8 个修改或新增路径 | 独有提交与未提交改动 |
| `CourseSchedule-morph-render-budget` | 1 | 干净 | 独有提交，先留作任务交接 |
| `CourseSchedule-perf-before` | 0 | 1 个修改文件、detached | 未提交改动 |

今后先核对对应任务状态和 `git status`，再处理其中已完成的干净工作树。含独有提交的分支先保留可追溯引用；含未提交改动的工作树应由原任务完成提交或明确归档补丁。不要为了让 `git worktree list` 变短而丢失这两类内容。

## 文档与交付边界

本轮更新入口 README、`AGENTS.md`、结构与设计系统文档，新增版本基线、功能地图、最初十个 Beta 与 Git 工作流；一份先前未归档的学校 SDK 调查移入 `docs/archive/`。HTML、完整照片、副本生成脚本和私人清理目录均在仓库外，不能随文档提交。该轮仅改文档，不需要 Android 编译；提交前检查链接、事实、`git diff --check` 与暂存清单。
