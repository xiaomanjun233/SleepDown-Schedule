# 1.2.5 正式版安装与发布记录

用户要求将版本改为正式版，合并 1.2.4 以来各个 Beta 的最终更新，再覆盖安装并发布。产品更新说明见 [1.2.5 更新日志](../release-notes/1.2.5.md)。

## 最终交付

- 版本 `1.2.5`，versionCode `32`，包名 `com.xiaomanjun.sleepdownschedule`。
- 应用内「关于应用 → 更新日志」加入与发布页一致的 15 项产品更新，日期为 `2026-09-14`；当前版本沿用默认展开行为。
- 发布源码提交 `233e72376cb0810c6426c269fa4f6c2d3c0c8db0`，GitHub 与 Gitee 标签 `v1.2.5` 指向该提交，源码同步至 `codex/mainline-preview-performance`。
- 初次安装后发现应用内日志遗漏，补齐后重新构建、安装，再发布最终安装包。正式 Release 未上传前，已将本轮刚创建的 GitHub 标签从准备提交更新至上述最终提交。

## 构建与安装

- 最终 `:app:assembleGithubRelease` 成功，耗时 5 分 9 秒，保留代码与资源压缩、lintVital 和签名打包；没有增加测试套件。
- APK v2 签名校验通过，并从最终 APK 中核对全部 15 项更新文案均已包含。
- PLJ110、Android 17 覆盖安装返回 `Success`；设备显示 `1.2.5 / 32`，更新时间 `2026-09-14 12:45:45`。保留数据，没有自动启动应用。
- 文件名 `SleepDown-Schedule_v1.2.5.apk`，大小 7,127,399 字节。
- SHA-256：`fa88625597e559f4bf77b3f715649521fc08dfb1e7b544e8d2fafb3ffba16732`。

## 发布验证

- [GitHub 正式版](https://github.com/xiaomanjun233/SleepDown-Schedule/releases/tag/v1.2.5)：`draft=false`、`prerelease=false`，已设为 latest。发布后通过 GitHub 插件核对源码提交、正式版状态、安装包大小与摘要。
- [Gitee 正式版](https://gitee.com/xiaomanjun233/SleepDown-Schedule/releases/tag/v1.2.5)：`prerelease=false`。从公开附件地址下载回读，大小和 SHA-256 与本地安装包一致。
- 应用使用的 Gitee 版本列表接口已返回 `v1.2.5`、完整更新日志及 APK 附件，发布文案与本地文件一致。
- GitHub 插件用于元数据读取；当前插件没有 Release 创建及二进制上传接口，上传沿用既有发布脚本和凭据连接。凭据只在进程内使用，不写入仓库或记录。
