<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application>
        <profileable
            android:shell="true"
            tools:targetApi="q" />
        <receiver
            android:name=".BenchmarkWallpaperReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.xiaomanjun.sleepdownschedule.benchmark.CONFIGURE_WALLPAPER" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
