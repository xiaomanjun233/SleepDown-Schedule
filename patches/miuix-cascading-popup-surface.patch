diff --git a/miuix-ui/src/commonMain/kotlin/top/yukonga/miuix/kmp/layout/CascadingListPopupLayout.kt b/miuix-ui/src/commonMain/kotlin/top/yukonga/miuix/kmp/layout/CascadingListPopupLayout.kt
index 0c7f6a2..3d558f4 100644
--- a/miuix-ui/src/commonMain/kotlin/top/yukonga/miuix/kmp/layout/CascadingListPopupLayout.kt
+++ b/miuix-ui/src/commonMain/kotlin/top/yukonga/miuix/kmp/layout/CascadingListPopupLayout.kt
@@ -77,6 +77,8 @@ internal fun CascadingListPopupLayout(
     onDismissRequest: () -> Unit,
     dropdownColors: DropdownColors,
     popupModifier: Modifier = Modifier,
+    surfaceModifier: Modifier = Modifier,
+    primarySurfaceModifier: Modifier = surfaceModifier,
     onDismissFinished: (() -> Unit)? = null,
     popupPositionProvider: PopupPositionProvider = ListPopupDefaults.DropdownPositionProvider,
     alignment: PopupPositionProvider.Align = PopupPositionProvider.Align.End,
@@ -305,10 +305,4 @@ internal fun CascadingListPopupLayout(
                         },
                         getAnchorBounds = { anchorBoundsByItem[it] },
-                        setAnchorBounds = { item, bounds ->
-                            // Freeze the anchor once secondary is shown so primary's scale
-                            // animation can't drift it via onGloballyPositioned.
-                            if (displayedItem == null) {
-                                anchorBoundsByItem[item] = bounds
-                            }
-                        },
+                        setAnchorBounds = { item, bounds -> anchorBoundsByItem[item] = bounds },
                         onPrimarySizeChange = { primarySize = it },
@@ -329,6 +323,8 @@ internal fun CascadingListPopupLayout(
                         layoutInfo = layoutInfo,
                         layoutDirection = layoutDirection,
                         surfaceColor = surfaceColor,
+                        surfaceModifier = surfaceModifier,
+                        primarySurfaceModifier = primarySurfaceModifier,
                         maskColor = maskColor,
                     )
                 }
@@ -377,6 +371,8 @@ private fun CascadingMorphSubLayout(
     layoutInfo: ListPopupLayoutInfo,
     layoutDirection: LayoutDirection,
     surfaceColor: Color,
+    surfaceModifier: Modifier,
+    primarySurfaceModifier: Modifier,
     maskColor: Color,
 ) {
     SubcomposeLayout(modifier = Modifier.fillMaxSize()) { constraints ->
@@ -418,6 +414,7 @@ private fun CascadingMorphSubLayout(
                 popupLayoutPosition = layoutInfo.popupLayoutPosition,
                 transformOrigin = layoutInfo.localTransformOrigin,
                 surfaceColor = surfaceColor,
+                primarySurfaceModifier = primarySurfaceModifier,
                 dropdownColors = dropdownColors,
                 primaryScale = primaryScale,
                 maskAlpha = maskAlpha,
@@ -484,6 +481,7 @@ private fun CascadingMorphSubLayout(
                         windowBounds = windowBounds,
                         layoutDirection = layoutDirection,
                     )
+                    val secondaryOpensUpward = secondaryRect.top < anchorRect.top
                     val primaryRect = IntRect(
                         left = primaryOffsetWindow.x,
                         top = primaryOffsetWindow.y,
@@ -511,6 +509,7 @@ private fun CascadingMorphSubLayout(
                             secondaryLocalInUnion = secondaryLocalInUnion,
                             anchorLocalInUnion = anchorLocalInUnion,
                             anchorPaddingTopPx = anchorPaddingTopPx,
+                            secondaryOpensUpward = secondaryOpensUpward,
                             isAnchorPopupLast = isAnchorPopupLast,
                             secondaryContentMaxHeight = resolvedMaxHeightPx,
                             enterFraction = enterFraction,
@@ -523,6 +522,7 @@ private fun CascadingMorphSubLayout(
                             onLeafSelected = onLeafSelected,
                             dropdownColors = dropdownColors,
                             surfaceColor = surfaceColor,
+                            surfaceModifier = surfaceModifier,
                         )
                     }.map { it.measure(unionConstraints) }
                     SecondaryLayout(placeables, unionRect)
@@ -584,11 +584,9 @@ internal fun computeSecondaryRect(
             else -> (windowBounds.right - secondarySize.width).coerceAtLeast(windowBounds.left)
         }
     }
-    val top = when {
-        anchor.top + secondarySize.height <= windowBounds.bottom -> anchor.top
-        anchor.bottom - secondarySize.height >= windowBounds.top -> anchor.bottom - secondarySize.height
-        else -> (windowBounds.bottom - secondarySize.height).coerceAtLeast(windowBounds.top)
-    }
+    val minTop = windowBounds.top
+    val maxTop = (windowBounds.bottom - secondarySize.height).coerceAtLeast(minTop)
+    val top = (anchor.bottom - secondarySize.height).coerceIn(minTop, maxTop)
     return IntRect(
         left = left,
         top = top,
diff --git a/miuix-ui/src/commonMain/kotlin/top/yukonga/miuix/kmp/layout/CascadingMorphContent.kt b/miuix-ui/src/commonMain/kotlin/top/yukonga/miuix/kmp/layout/CascadingMorphContent.kt
index 3c496e1..450b735 100644
--- a/miuix-ui/src/commonMain/kotlin/top/yukonga/miuix/kmp/layout/CascadingMorphContent.kt
+++ b/miuix-ui/src/commonMain/kotlin/top/yukonga/miuix/kmp/layout/CascadingMorphContent.kt
@@ -67,6 +67,7 @@ internal fun CascadingPrimaryContent(
     popupLayoutPosition: PopupLayoutPosition,
     transformOrigin: TransformOrigin,
     surfaceColor: Color,
+    primarySurfaceModifier: Modifier,
     dropdownColors: DropdownColors,
     primaryScale: () -> Float,
     maskAlpha: () -> Float,
@@ -94,6 +95,11 @@ internal fun CascadingPrimaryContent(
             .dropShadow(shape = shape, shadow = CascadeShadow)
             .background(color = surfaceColor),
     ) {
+        Box(
+            modifier = Modifier
+                .matchParentSize()
+                .then(primarySurfaceModifier)
+        )
         Box {
             ListPopupColumn {
                 val lastEntryIdx = entries.lastIndex
@@ -212,6 +218,7 @@ internal fun CascadingSecondaryContent(
     secondaryLocalInUnion: IntRect,
     anchorLocalInUnion: IntRect,
     anchorPaddingTopPx: Int,
+    secondaryOpensUpward: Boolean,
     isAnchorPopupLast: Boolean,
     secondaryContentMaxHeight: Int,
     enterFraction: () -> Float,
@@ -224,9 +231,18 @@ internal fun CascadingSecondaryContent(
     onLeafSelected: (DropdownItem) -> Unit,
     dropdownColors: DropdownColors,
     surfaceColor: Color,
+    surfaceModifier: Modifier,
 ) {
     val cornerPx = with(LocalDensity.current) { CascadingPopupCornerRadius.toPx() }
-    val anchorTopLeft = anchorLocalInUnion.topLeft
+    // When the submenu opens upward its header is the trailing row. Offset the collapsed
+    // content by the body height so that header remains exactly over the primary trigger row.
+    val collapsedContentTopLeft = if (secondaryOpensUpward) {
+        anchorLocalInUnion.topLeft.copy(
+            y = anchorLocalInUnion.bottom - secondaryLocalInUnion.height,
+        )
+    } else {
+        anchorLocalInUnion.topLeft
+    }
     val secondaryTopLeft = secondaryLocalInUnion.topLeft
    val secondaryWidth = secondaryLocalInUnion.width

@@ -289,6 +305,7 @@ internal fun CascadingSecondaryContent(
                     }
                 }
             }
+            .then(surfaceModifier)
             // Occupy full union size so the inner content can slide between anchor and
             // secondary even when the anchor sits outside the secondary bounds.
             .layout { measurable, _ ->
@@ -305,12 +322,12 @@ internal fun CascadingSecondaryContent(
                 .graphicsLayer {
                     val frac = expandFraction()
                     translationX = lerp(
-                        anchorTopLeft.x.toFloat(),
+                        collapsedContentTopLeft.x.toFloat(),
                         secondaryTopLeft.x.toFloat(),
                         frac,
                     )
                     translationY = lerp(
-                        anchorTopLeft.y.toFloat(),
+                        collapsedContentTopLeft.y.toFloat(),
                         secondaryTopLeft.y.toFloat(),
                         frac,
                     )
@@ -338,6 +355,7 @@ internal fun CascadingSecondaryContent(
                 arrowRotation = arrowRotation,
                 anchorHeightPx = anchorLocalInUnion.height,
                 anchorPaddingTopPx = anchorPaddingTopPx,
+                headerAtBottom = secondaryOpensUpward,
                 onHeaderClick = onCollapseSecondary,
                 onLeafSelected = onLeafSelected,
             )
@@ -355,11 +373,36 @@ internal fun CascadingSecondaryColumn(
     arrowRotation: () -> Float,
     anchorHeightPx: Int,
     anchorPaddingTopPx: Int,
+    headerAtBottom: Boolean = false,
     onHeaderClick: () -> Unit,
     onLeafSelected: (DropdownItem) -> Unit,
 ) {
     val children = triggerItem.children.orEmpty()
     ListPopupColumn {
+        if (headerAtBottom) {
+            children.forEachIndexed { index, child ->
+                key(child) {
+                    Box(modifier = Modifier.fillMaxWidth(), propagateMinConstraints = true) {
+                        DropdownImpl(
+                            item = child,
+                            optionSize = children.size,
+                            isSelected = child.selected,
+                            index = index,
+                            dropdownColors = dropdownColors,
+                            enabled = child.enabled,
+                            hasSubmenu = false,
+                            isFirst = index == 0,
+                            isLast = false,
+                            onSelectedIndexChange = { onLeafSelected(child) },
+                        )
+                    }
+                }
+            }
+            HorizontalDivider(
+                modifier = Modifier.padding(horizontal = 20.dp, vertical = 4.dp),
+                thickness = 1.5.dp,
+            )
+        }
         MorphHeaderRow(
             triggerItem = triggerItem,
             arrowRotation = arrowRotation,
@@ -369,6 +412,7 @@ internal fun CascadingSecondaryColumn(
             dropdownColors = dropdownColors,
             onClick = onHeaderClick,
         )
+        if (!headerAtBottom) {
         HorizontalDivider(
             modifier = Modifier
                 .padding(horizontal = 20.dp, vertical = 4.dp),
@@ -397,5 +441,6 @@ internal fun CascadingSecondaryColumn(
                 }
             }
         }
+        }
     }
 }
diff --git a/miuix-ui/src/commonMain/kotlin/top/yukonga/miuix/kmp/overlay/OverlayCascadingListPopup.kt b/miuix-ui/src/commonMain/kotlin/top/yukonga/miuix/kmp/overlay/OverlayCascadingListPopup.kt
index 0124fe2..ce9ee7b 100644
--- a/miuix-ui/src/commonMain/kotlin/top/yukonga/miuix/kmp/overlay/OverlayCascadingListPopup.kt
+++ b/miuix-ui/src/commonMain/kotlin/top/yukonga/miuix/kmp/overlay/OverlayCascadingListPopup.kt
@@ -29,6 +29,8 @@ import top.yukonga.miuix.kmp.utils.MiuixPopupUtils.Companion.PopupLayout
  * @param onDismissRequest Invoked when the popup wants to be dismissed.
  * @param onDismissFinished Invoked after the exit animation finishes.
  * @param popupModifier Modifier applied to the popup body.
+ * @param surfaceModifier Modifier applied to the animated popup surfaces.
+ * @param primarySurfaceModifier Modifier applied to the primary popup's background layer.
  * @param popupPositionProvider Position strategy for the primary popup relative to its anchor.
  * @param alignment Alignment of the primary popup.
  * @param enableWindowDim Whether to dim the rest of the window while the popup is shown.
@@ -44,6 +46,8 @@ fun OverlayCascadingListPopup(
     entries: List<DropdownEntry>,
     onDismissRequest: () -> Unit,
     popupModifier: Modifier = Modifier,
+    surfaceModifier: Modifier = Modifier,
+    primarySurfaceModifier: Modifier = surfaceModifier,
     onDismissFinished: (() -> Unit)? = null,
     popupPositionProvider: PopupPositionProvider = ListPopupDefaults.DropdownPositionProvider,
     alignment: PopupPositionProvider.Align = PopupPositionProvider.Align.End,
@@ -74,6 +78,8 @@ fun OverlayCascadingListPopup(
         onDismissRequest = onDismissRequest,
         onDismissFinished = onDismissFinished,
         popupModifier = popupModifier,
+        surfaceModifier = surfaceModifier,
+        primarySurfaceModifier = primarySurfaceModifier,
         popupPositionProvider = popupPositionProvider,
         alignment = alignment,
         enableWindowDim = enableWindowDim,
diff --git a/miuix-ui/src/commonMain/kotlin/top/yukonga/miuix/kmp/window/WindowCascadingListPopup.kt b/miuix-ui/src/commonMain/kotlin/top/yukonga/miuix/kmp/window/WindowCascadingListPopup.kt
index 44e76a1..1c77d8f 100644
--- a/miuix-ui/src/commonMain/kotlin/top/yukonga/miuix/kmp/window/WindowCascadingListPopup.kt
+++ b/miuix-ui/src/commonMain/kotlin/top/yukonga/miuix/kmp/window/WindowCascadingListPopup.kt
@@ -29,6 +29,8 @@ import top.yukonga.miuix.kmp.utils.platformDialogProperties
  * @param onDismissRequest Invoked when the popup wants to be dismissed.
  * @param onDismissFinished Invoked after the exit animation finishes.
  * @param popupModifier Modifier applied to the popup body.
+ * @param surfaceModifier Modifier applied to the animated popup surfaces.
+ * @param primarySurfaceModifier Modifier applied to the primary popup's background layer.
  * @param popupPositionProvider Position strategy for the primary popup relative to its anchor.
  * @param alignment Alignment of the primary popup.
  * @param enableWindowDim Whether to dim the rest of the window while the popup is shown.
@@ -43,6 +45,8 @@ fun WindowCascadingListPopup(
     entries: List<DropdownEntry>,
     onDismissRequest: () -> Unit,
     popupModifier: Modifier = Modifier,
+    surfaceModifier: Modifier = Modifier,
+    primarySurfaceModifier: Modifier = surfaceModifier,
     onDismissFinished: (() -> Unit)? = null,
     popupPositionProvider: PopupPositionProvider = ListPopupDefaults.DropdownPositionProvider,
     alignment: PopupPositionProvider.Align = PopupPositionProvider.Align.End,
@@ -71,6 +75,8 @@ fun WindowCascadingListPopup(
         onDismissRequest = onDismissRequest,
         onDismissFinished = onDismissFinished,
         popupModifier = popupModifier,
+        surfaceModifier = surfaceModifier,
+        primarySurfaceModifier = primarySurfaceModifier,
         popupPositionProvider = popupPositionProvider,
         alignment = alignment,
         enableWindowDim = enableWindowDim,
